pyserial>=3.5
# Note: tkinter is required but cannot be installed via pip.
# On Linux: sudo apt install python3-tk (Debian/Ubuntu)
# On Windows/macOS: tkinter is included with the Python standard library.
